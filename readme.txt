OpenGL version = 4.3
OpenGl profile = core

To compile in Windows, you must have MinGW installed and run "mingw32-make" in the folder.
In other OS, you might need GCC instead, with the command "make". You also have to change the shell commands inside the makefile before running "make".
